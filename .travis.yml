sudo: false
language: python
before_install: sudo apt-get install python-tk python3-tk ghostscript
# command to install dependencies
install: "make"
cache: pip
jobs:
  include:
    - stage: test
      script:
        - make test
      python: '2.7'
    - stage: test
      script:
        - make test
      python: '3.5'
    - stage: test
      script:
        - make test
      python: '3.6'
    - stage: test
      script:
        - make test
      python: '3.7'
      dist: xenial
      sudo: true     
    - stage: coverage
      python: 3.6
      script: make test && codecov --verbose
